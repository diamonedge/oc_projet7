[DEFAULT]
MongoDbUri = mongodb://${MONGODB_USER}:${MONGODB_PASSWORD}@${MONGODB_HOST}:27017/
Db_name = NosCites
Collection_Name = listing_paris
TempDir = injection
BatchSize = 500
[TEST]
EXPECTED_FIELDS = ${EXPECTED_FIELDS}
EXPECTED_FIELD_COUNT = ${EXPECTED_FIELD_COUNT}
[USERS_ROLES]
RANDOM_PASSWORD1=$(tr -dc A-Za-z0-9 </dev/urandom | head -c 13)
READER_USER_NAME = reader_user
READER_USER_PASSWORD = ${RANDOM_PASSWORD1}
